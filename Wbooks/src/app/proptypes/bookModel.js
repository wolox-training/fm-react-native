import { string, number } from 'prop-types';

const bookModel = {
  author: string,
  genre: string,
  id: number,
  image_url: string,
  publisher: string,
  title: string,
  year: string
};

export default bookModel;
